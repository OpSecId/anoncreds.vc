{% extends('layouts/base.jinja') %}
{% block page %}
<div class="card">
    <div class="card-body">
        {# <div class="row align-items-center"> #}
            {# <div class="col-4">
                <img src="{{ qrcode(session['invitation'], error_correction='H', border=1) }}" alt="" class="rounded">
            </div> #}
            {# <div class="col"> #}
                <h3 class="card-title mb-1">
                    <div class="text-reset">AnonCreds & WebVH</div>
                </h3>
                <div class="text-secondary"><a target="_blank" href="{{session['demo'].get('schema_url')}}">Schema</a>
                </div>
                <div class="text-secondary"><a target="_blank" href="{{session['demo'].get('cred_def_url')}}">Credential
                        Definition</a></div>
                <div class="text-secondary"><a target="_blank" href="{{session['demo'].get('rev_def_url')}}">Revocation
                        Registry</a></div>
            {# </div> #}
        {# </div> #}
    </div>
    <div class="card-footer">
        <div class="row g-2 align-items-center">
            <div class="col-6"><a href="#" onclick="credentialOffer()"
                    class="btn btn-sm btn-outline-dark w-100">Credential Offer</a></div>
            <div class="col-6"><a href="#" onclick="presentationRequest()"
                    class="btn btn-sm btn-outline-dark w-100">Presentation Request</a></div>
        </div>
    </div>
</div>

<script>
    function credentialOffer() {
        $.get("{{ session['endpoint'] }}/offer?client_id={{ session['client_id'] }}")
    }
    function presentationRequest() {
        $.get("{{session['endpoint']}}/request?client_id={{session['client_id']}}")
    }
</script>
{% endblock %}