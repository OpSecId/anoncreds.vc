label: AnonCreds WebVH Demo

admin: [0.0.0.0, 8020]
# Pass --admin-api-key at runtime to match AGENT_ADMIN_API_KEY.
# Leave this on only for local bring-up.
admin-insecure-mode: true

inbound-transport:
  - [ws, 0.0.0.0, 8021]
  - [http, 0.0.0.0, 8022]
outbound-transport: [ws, http]
endpoint:
  - https://demo.cloudagent.agency

auto-provision: true
recreate-wallet: false
no-ledger: true

wallet-type: askar-anoncreds
wallet-name: anoncreds-vc
wallet-key: insecure
wallet-allow-insecure-seed: true

auto-ping-connection: true
auto-accept-invites: true
auto-accept-requests: true
auto-respond-messages: true

auto-store-credential: true
auto-respond-credential-offer: true
auto-respond-credential-request: true
auto-respond-credential-proposal: true

auto-verify-presentation: true
auto-respond-presentation-request: true

preserve-exchange-records: true

plugin:
  - webvh
  - status_list.v1_0
  - connection_update.v1_0

plugin-config-value:
  - webvh.server_url=https://sandbox.bcvh.vonx.io
